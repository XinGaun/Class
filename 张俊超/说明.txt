新增加的JSP和类的说明
1.staff类新加了了一个字段staffstatus int not null--员工状态
2.zjc.entity与zhangjunchao两个包是同一级别都在src目录下
3.Hlogin.jsp和_blank.jsp修改了，它们是同一级别，都在xmking目录下
4.添加了一个单个的方法查询的方法
/**
	 * 查询单个方法
	 * @param sql 查询sql语句
	 * @param o object数组
	 * @return list集合
	 */
	public int executeQuerysingle(String sql){
		//List objectList=new ArrayList();
		try {
			Connection con = getConnection();
			PreparedStatement ps = con.prepareStatement(sql);
			ResultSet rs= ps.executeQuery();
			rs.next();
			int a = rs.getInt(1);
			return a;
			
		} catch (Exception e) {
			// TODO: handle exception
			System.out.println("1:"+e.getMessage());
		}
		return 0;
	}  